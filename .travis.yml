sudo: true
language: python
python:
  - "2.7"
#  - "pypy"
# command to install dependencies
install: 
  - "python setup.py install"
# command to run tests
script: 
  - "sudo ~/virtualenv/python2.7/bin/doorpi_cli --test --trace"
